const Message = require('./models/message');

class MongoDBManager {
    constructor() {
        // lógica necesaria para la conexión y gestión de datos con MongoDB
    }

    //   métodos para la gestión de datos con MongoDB
}

module.exports = MongoDBManager;