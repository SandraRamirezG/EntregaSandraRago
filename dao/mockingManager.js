const generateMockProducts = () => {
    // Lógica para generar productos simulados (puedes usar bibliotecas como faker.js)
    const mockProducts = [];
    for (let i = 0; i < 100; i++) {
        mockProducts.push({
            // Formato de producto simulado
            // ...
        });
    }
    return mockProducts;
};

module.exports = { generateMockProducts };