const fs = require('fs');

class FileSystemManager {
    constructor() {
        //  lógica necesaria para la gestión de datos con FileSystem
    }

    //  métodos para la gestión de datos con FileSystem
}

module.exports = FileSystemManager;